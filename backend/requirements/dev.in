# Direct dev/test-only dependencies, layered on top of the runtime deps.
# Edit this file, then run `make compile-deps` to regenerate the lockfiles.
#
# The compiled dev.txt is a superset of base.txt (runtime + dev). The
# local/CI test image installs dev.txt.

-r base.in

factory-boy==3.3.3
moto==5.1.22
coverage==7.13.5
